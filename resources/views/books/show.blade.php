<x-layouts.app :title="$book->title"><main class="container" style="padding:70px 0;max-width:800px">@if(session('book_success'))<div class="home-alert success">{{ session('book_success') }}</div>@endif @error('payment')<div class="home-alert error">{{ $message }}</div>@enderror<h1>{{ $book->title }}</h1><p>Par {{ $book->author }} · {{ $book->published_on->format('d/m/Y') }}</p><p>{{ $book->description }}</p><h2>{{ number_format($book->price,0,',',' ') }} FCFA</h2><a class="solid-submit" href="{{ route('books.begin',$book) }}">Acheter ce livre</a></main></x-layouts.app>
