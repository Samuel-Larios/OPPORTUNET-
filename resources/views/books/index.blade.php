@php $isFrench = app()->getLocale() === 'fr'; @endphp
<x-layouts.app :title="$isFrench ? 'Livres' : 'Books'" :show-hero="false"><main class="cv-services-page"><section class="cv-services-hero"><div class="container"><div class="cv-services-hero-shell reveal"><div class="cv-services-hero-copy"><span class="section-label">{{ $isFrench ? 'Librairie numérique' : 'Digital bookstore' }}</span><h1 class="section-title">{{ $isFrench ? 'Des livres pour éclairer votre parcours' : 'Books to guide your journey' }}</h1><p class="section-sub">{{ $isFrench ? 'Achetez vos livres en toute sécurité et recevez votre exemplaire PDF personnel par e-mail.' : 'Purchase securely and receive your personal PDF copy by email.' }}</p></div></div></div></section><section class="cv-services-list-section"><div class="container"><div class="home-section-head reveal"><span class="section-label">{{ $isFrench ? 'Catalogue' : 'Catalogue' }}</span><h2 class="section-title">{{ $isFrench ? 'Nos livres disponibles' : 'Available books' }}</h2></div><div class="cv-services-grid">@forelse($books as $index => $book)<article class="service-card service-card-{{ ['blue','gold','teal','slate'][$index%4] }} reveal"><a href="{{ route('books.show',$book) }}">@if($book->cover_path)<img src="{{ \Illuminate\Support\Facades\Storage::disk('public')->url($book->cover_path) }}" alt="{{ $book->title }}" style="width:100%;height:260px;object-fit:cover;border-radius:14px">@endif</a><span class="service-meta">{{ $book->author }}</span><h3>{{ $book->title }}</h3><p>{{ \Illuminate\Support\Str::limit($book->description,150) }}</p><div class="cv-service-footer"><strong>{{ number_format($book->price,0,',',' ') }} FCFA</strong><a class="opportunity-link" href="{{ route('books.show',$book) }}">{{ $isFrench ? 'Voir le livre' : 'View book' }}</a></div></article>@empty<p class="panel-empty">{{ $isFrench ? 'Aucun livre n’est disponible pour le moment.' : 'No books are available yet.' }}</p>@endforelse</div></div></section></main></x-layouts.app>
